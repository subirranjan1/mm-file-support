<%= form_for(@movement_group) do |f| %>
  <% if @movement_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@movement_group.errors.count, "error") %> prohibited this movement_stream from being saved:</h2>

      <ul>
      <% @movement_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
	<%= f.label :project %>
	<%= f.collection_select :project_id, @projects, :id, :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

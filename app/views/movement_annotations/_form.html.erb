<%= form_for(@movement_annotation) do |f| %>
  <% if @movement_annotation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@movement_annotation.errors.count, "error") %> prohibited this movement_annotation from being saved:</h2>

      <ul>
      <% @movement_annotation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :format %><br>
    <%= f.text_field :format %>
  </div>
  <div class="field">
	<%= f.label :movement_datum%>
	<%= f.collection_select :movement_datum_id, @movement_data, :id, :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
